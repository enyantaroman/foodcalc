import { Button } from "@mui/material";

const UpdateButton = () => {
    return (
        <Button
        variant="contained"
        >
            更新
        </Button>
    )
}

export default UpdateButton;