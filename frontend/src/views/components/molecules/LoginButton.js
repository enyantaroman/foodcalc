import { Button } from "@mui/material";

const LoginButton = () => {
    return (
        <Button
        variant="contained"
        >
            ログイン
        </Button>
    )
}

export default LoginButton;